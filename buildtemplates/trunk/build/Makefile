include ../etc/Makefile.global

all :
	@: no default actions

# install :
# 	@ mkdir -p $(BUILDDIR)
# 	cp Makefile.global makedefs installdefs $(BUILDDIR)
#	@ mkdir -p $(BUILDDIR)
#	cp *.m4 $(BUILDDIR)

clean:
	$(RM) *~ core*

distclean: clean
	@: nothing extra to do
